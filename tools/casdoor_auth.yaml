identity:
  name: casdoor_auth
  author: MohmdFo <mohammad.fotouhi80@gmail.com>
  label:
    en_US: Casdoor Authentication
  description:
    human:
      en_US: A tool to generate Casdoor OAuth URLs for login or signup.
    llm: Generate a Casdoor authentication URL.
parameters:
  - name: action
    type: string
    required: true
    label:
      en_US: Action
    human_description:
      en_US: Specify "login" to get the login URL or "signup" for the signup URL.
    llm_description: Use "login" or "signup" to generate the corresponding OAuth URL.
    form: llm
extra:
  python:
    source: tools/casdoor_auth.py
